spring:
  datasource:
    url: jdbc:mysql://localhost:3307/book_borrow_system?createDatabaseIfNotExist = true
    username: root
    password: //add password

  jpa:
    hibernate:
      ddl-auto: update
      show_sql: true